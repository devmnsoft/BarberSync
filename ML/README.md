# ML Module
- ML.NET model pipeline for service recognition.
- OpenCV preprocessing (face/hair region extraction).
- Optional Azure Cognitive Services adapter.
- Expose predictions to `ServiceRecognition` endpoint.
