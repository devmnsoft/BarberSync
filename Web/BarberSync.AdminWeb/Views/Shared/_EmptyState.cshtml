@model (string Title, string Message)
<div class="state-card state-empty text-center p-4">
    <i class="bi bi-inbox display-5"></i>
    <h4 class="mt-2">@Model.Title</h4>
    <p class="text-muted mb-0">@Model.Message</p>
</div>
