@model (string Title, string Message)
<div class="state-card state-error text-center p-4">
    <i class="bi bi-exclamation-triangle display-5"></i>
    <h4 class="mt-2">@Model.Title</h4>
    <p class="mb-0">@Model.Message</p>
</div>
