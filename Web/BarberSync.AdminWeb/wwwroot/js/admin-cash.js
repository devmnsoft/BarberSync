(() => { const store=()=>window.BarberSyncDemoStore; const bus=()=>window.BarberSyncEventBus; const row=(k,v)=>`<div class="saas-row"><strong>${k}</strong><span>${v}</span></div>`; function render(){ if(!document.querySelector('[data-cash-30]'))return; const p=store()?.get('payments')||[]; const sum=m=>p.filter(x=>x.method===m).reduce((a,x)=>a+(x.amount||0),0); document.querySelector('[data-cash-summary]').innerHTML=row('Status','Aberto')+row('PIX',`R$ ${sum('PIX').toFixed(2)}`)+row('Cartão',`R$ ${sum('Cartão').toFixed(2)}`)+row('Dinheiro',`R$ ${sum('Dinheiro').toFixed(2)}`)+row('Entradas',p.length)+row('Saídas/Sangria','R$ 120,00')+row('Divergência demo','R$ 0,00'); } document.addEventListener('click',e=>{ if(e.target.closest('[data-cash-open],[data-cash-supply],[data-cash-withdraw],[data-cash-close]')) bus()?.emit('payment:created',{title:'Movimento de caixa registrado',module:'Caixa'}); render(); }); window.addEventListener('barbersync:event',render); document.addEventListener('DOMContentLoaded',render); })();
