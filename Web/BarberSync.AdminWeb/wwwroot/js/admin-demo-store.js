(() => {
  const STORAGE_KEY = 'BarberSync:AdminDemoStore:v3';
  const SCENARIO_KEY = 'BarberSync:AdminDemoStore:scenario';
  const today = new Date().toISOString().slice(0, 10);
  const money = value => Number(value || 0);
  const clone = value => JSON.parse(JSON.stringify(value));
  const id = prefix => `${prefix}-${Date.now()}-${Math.random().toString(16).slice(2, 7)}`;
  const toast = (message, type = 'success') => window.AdminToast?.show?.(message, type);

  const baseSeed = {
    clients: [
      { id: 'cli-001', name: 'Lucas Almeida', segment: 'VIP', personType: 'PF', document: '123.456.789-00', phone: '(11) 98888-1111', whatsapp: '(11) 98888-1111', email: 'lucas@demo.com', preferredService: 'Corte + Barba', preferredProfessional: 'Rafael Barber', status: 'VIP', vip: 'Sim', cashback: 48, totalSpent: 1860, score: 96, nextBestAction: 'Agendar retorno em 21 dias', preferences: 'Fade baixo, barba desenhada, finalização com pomada matte' },
      { id: 'cli-002', name: 'Marina Costa', segment: 'Aniversariante', personType: 'PF', document: '987.654.321-00', phone: '(11) 97777-2222', whatsapp: '(11) 97777-2222', email: 'marina@demo.com', preferredService: 'Design de sobrancelha', preferredProfessional: 'Camila Beauty', status: 'Ativo', cashback: 22, totalSpent: 740, score: 84, nextBestAction: 'Enviar cupom de aniversário', preferences: 'Horários à tarde e contato por WhatsApp' },
      { id: 'cli-003', name: 'André Martins', segment: 'Inativo', personType: 'PF', document: '456.789.123-00', phone: '(11) 96666-3333', email: 'andre@demo.com', preferredService: 'Barba Tradicional', preferredProfessional: 'Rafael Barber', status: 'Reativar', cashback: 12, totalSpent: 510, score: 58, nextBestAction: 'Campanha de reativação', preferences: 'Prefere pagamento PIX' },
      { id: 'cli-004', name: 'Paula Nunes', segment: 'Novo', phone: '(11) 95555-4444', email: 'paula@demo.com', preferredService: 'Hidratação Premium', preferredProfessional: 'Camila Beauty', status: 'Novo', cashback: 8, totalSpent: 160, score: 72, nextBestAction: 'Oferecer pacote recorrente', preferences: 'Produtos sem fragrância forte' }
    ],
    professionals: [
      { id: 'pro-001', name: 'Rafael Barber', specialty: 'Corte degradê e barba', phone: '(11) 95555-1000', email: 'rafael@barbersync.demo', commission: 40, status: 'Disponível', monthlyRevenue: 18450, rating: 4.9, monthlyGoal: 22000, occupancy: 91, appointmentsToday: 8, topServices: 'Corte + Barba, Barba Tradicional' },
      { id: 'pro-002', name: 'Camila Beauty', specialty: 'Estética masculina', phone: '(11) 95555-2000', email: 'camila@barbersync.demo', commission: 35, status: 'Em atendimento', monthlyRevenue: 12680, rating: 4.8, monthlyGoal: 18000, occupancy: 78, appointmentsToday: 6, topServices: 'Sobrancelha, Limpeza de pele' },
      { id: 'pro-003', name: 'Diego Fade', specialty: 'Corte moderno', phone: '(11) 95555-3000', commission: 38, status: 'Disponível', monthlyRevenue: 9800, rating: 4.7, monthlyGoal: 16000, occupancy: 62, appointmentsToday: 4, topServices: 'Corte Masculino' }
    ],
    services: [
      { id: 'srv-001', name: 'Corte + Barba', category: 'Combo premium', description: 'Corte, barba quente e finalização.', price: 95, durationMinutes: 70, commission: 40, site: 'Sim', kiosk: 'Sim', mobile: 'Sim', status: 'Ativo', margin: 72, sold: 148, productsConsumed: [{ productId: 'prd-001', quantity: 1 }, { productId: 'prd-002', quantity: 1 }] },
      { id: 'srv-002', name: 'Corte Masculino', category: 'Cabelo', description: 'Corte clássico ou degradê.', price: 65, durationMinutes: 45, commission: 35, site: 'Sim', kiosk: 'Sim', mobile: 'Sim', status: 'Ativo', margin: 76, sold: 210, productsConsumed: [{ productId: 'prd-001', quantity: 1 }] },
      { id: 'srv-003', name: 'Barba Tradicional', category: 'Barba', description: 'Toalha quente, navalha e balm.', price: 45, durationMinutes: 30, commission: 35, site: 'Sim', kiosk: 'Sim', mobile: 'Sim', status: 'Ativo', margin: 68, sold: 132, productsConsumed: [{ productId: 'prd-002', quantity: 1 }] },
      { id: 'srv-004', name: 'Hidratação Premium', category: 'Tratamento', description: 'Tratamento capilar profissional.', price: 120, durationMinutes: 60, commission: 36, site: 'Sim', kiosk: 'Não', mobile: 'Sim', status: 'Ativo', margin: 61, sold: 44, productsConsumed: [{ productId: 'prd-003', quantity: 1 }] }
    ],
    appointments: [
      { id: 'apt-001', clientId: 'cli-001', serviceId: 'srv-001', professionalId: 'pro-001', client: 'Lucas Almeida', service: 'Corte + Barba', professional: 'Rafael Barber', date: today, time: '10:30', status: 'Confirmado', channel: 'Admin', timeline: ['Agendamento confirmado'] },
      { id: 'apt-002', clientId: 'cli-002', serviceId: 'srv-004', professionalId: 'pro-002', client: 'Marina Costa', service: 'Hidratação Premium', professional: 'Camila Beauty', date: today, time: '14:00', status: 'Check-in', channel: 'Totem', timeline: ['Check-in feito no Totem'] },
      { id: 'apt-003', clientId: 'cli-003', serviceId: 'srv-003', professionalId: 'pro-001', client: 'André Martins', service: 'Barba Tradicional', professional: 'Rafael Barber', date: today, time: '18:30', status: 'Agendado', channel: 'PublicWeb', timeline: ['Solicitado pelo PublicWeb'] },
      { id: 'apt-004', clientId: 'cli-004', serviceId: 'srv-004', professionalId: 'pro-002', client: 'Paula Nunes', service: 'Hidratação Premium', professional: 'Camila Beauty', date: today, time: '16:00', status: 'Lista de espera', channel: 'Mobile', timeline: ['Entrou na lista de espera'] }
    ],
    serviceOrders: [
      { id: 'so-1001', appointmentId: 'apt-001', clientId: 'cli-001', serviceId: 'srv-001', professionalId: 'pro-001', name: 'Comanda #1001', client: 'Lucas Almeida', service: 'Corte + Barba', professional: 'Rafael Barber', items: [{ type: 'service', name: 'Corte + Barba', amount: 95 }, { type: 'product', productId: 'prd-001', name: 'Pomada Modeladora', amount: 39, quantity: 1 }], subtotal: 134, discount: 10, cashback: 5, total: 119, paymentMethod: 'PIX', status: 'Aberta', channel: 'Admin' },
      { id: 'so-1002', appointmentId: 'apt-002', clientId: 'cli-002', serviceId: 'srv-004', professionalId: 'pro-002', name: 'Comanda #1002', client: 'Marina Costa', service: 'Hidratação Premium', professional: 'Camila Beauty', items: [{ type: 'service', name: 'Hidratação Premium', amount: 120 }], subtotal: 120, discount: 0, cashback: 0, total: 120, paymentMethod: 'Cartão', status: 'Aguardando pagamento', channel: 'Totem' },
      { id: 'so-1003', clientId: 'cli-004', serviceId: 'srv-004', professionalId: 'pro-002', name: 'Comanda #1003', client: 'Paula Nunes', service: 'Hidratação Premium', professional: 'Camila Beauty', items: [{ type: 'service', name: 'Hidratação Premium', amount: 120 }, { type: 'product', productId: 'prd-003', name: 'Shampoo Antiqueda', amount: 59, quantity: 1 }], subtotal: 179, discount: 15, cashback: 8, total: 156, paymentMethod: 'Misto', status: 'Paga', channel: 'Mobile', paidAt: new Date().toISOString() }
    ],
    products: [
      { id: 'prd-001', name: 'Pomada Modeladora', product: 'Pomada Modeladora', quantity: 4, stock: 4, minimumStock: 12, cost: 22, price: 39, status: 'Crítico', suggestion: 'Comprar 24 unidades', turnover: 'Alto', movement: 'Saída' },
      { id: 'prd-002', name: 'Balm para barba', product: 'Balm para barba', quantity: 18, stock: 18, minimumStock: 10, cost: 18, price: 34, status: 'Normal', suggestion: 'Estoque saudável', turnover: 'Médio', movement: 'Entrada' },
      { id: 'prd-003', name: 'Shampoo Antiqueda', product: 'Shampoo Antiqueda', quantity: 8, stock: 8, minimumStock: 10, cost: 30, price: 59, status: 'Atenção', suggestion: 'Comprar 12 unidades', turnover: 'Médio', movement: 'Ajuste' }
    ],
    stockMovements: [{ id: 'mov-001', productId: 'prd-001', product: 'Pomada Modeladora', type: 'Saída', quantity: 4, reason: 'Venda em comanda', createdAt: today }],
    campaigns: [{ id: 'cmp-001', name: 'Retorno em 21 dias', audience: 'Clientes sem visita há 30 dias', period: '02/06 a 30/06', channel: 'WhatsApp', status: 'Ativa', result: '18 reativações', impacted: 240, estimatedRevenue: 7200, funnel: '240 > 98 > 31 > 18' }],
    coupons: [{ id: 'cup-001', code: 'RETORNO20', discount: '20% no combo', validUntil: '2026-06-30', usage: '32/100', status: 'Ativo' }],
    reviews: [{ id: 'rev-001', clientId: 'cli-001', serviceOrderId: 'so-1003', name: 'Lucas Almeida', rating: 5, nps: 10, comment: 'Atendimento excelente e pontual.', status: 'Promotor' }, { id: 'rev-002', name: 'Cliente detrator demo', rating: 3, nps: 6, comment: 'Fila demorou no sábado.', status: 'Detrator' }],
    loyalty: [{ id: 'loy-001', clientId: 'cli-001', name: 'Lucas Almeida', cashback: 48, expiring: 12, status: 'Saldo ativo' }, { id: 'loy-002', clientId: 'cli-002', name: 'Marina Costa', cashback: 22, expiring: 0, status: 'Saldo ativo' }],
    copilot: [{ id: 'cop-001', category: 'Estoque', priority: 'Alta', summary: 'Pomada Modeladora está em estoque crítico.', metric: '4 unidades / mínimo 12', action: 'Gerar reposição', status: 'Pendente' }, { id: 'cop-002', category: 'Clientes', priority: 'Média', summary: 'Há 8 clientes sem retorno há mais de 30 dias.', metric: 'R$ 7,2 mil potencial', action: 'Criar campanha de retorno', status: 'Pendente' }, { id: 'cop-003', category: 'Agenda', priority: 'Média', summary: 'Amanhã há horários vazios.', metric: '15h, 16h e 17h', action: 'Criar campanha para amanhã', status: 'Pendente' }],
    receipts: [],
    timeline: []
  };

  const aliases = { Clients: 'clients', clients: 'clients', Professionals: 'professionals', professionals: 'professionals', Services: 'services', services: 'services', Appointments: 'appointments', appointments: 'appointments', ServiceOrders: 'serviceOrders', serviceOrders: 'serviceOrders', serviceorders: 'serviceOrders', Stock: 'products', stock: 'products', Products: 'products', products: 'products', Campaigns: 'campaigns', campaigns: 'campaigns', Coupons: 'coupons', coupons: 'coupons', Reviews: 'reviews', reviews: 'reviews', Loyalty: 'loyalty', loyalty: 'loyalty', Copilot: 'copilot', copilot: 'copilot', Dashboard: 'dashboard', dashboard: 'dashboard', stockMovements: 'stockMovements' };
  const moduleKey = moduleName => aliases[moduleName] || aliases[String(moduleName || '').toLowerCase()] || moduleName;

  const scenarioMutators = {
    default: s => s,
    busyDay: s => { s.appointments.push(...Array.from({ length: 8 }, (_, i) => ({ id: `apt-busy-${i}`, client: `Cliente Movimento ${i + 1}`, service: i % 2 ? 'Corte Masculino' : 'Corte + Barba', professional: i % 2 ? 'Diego Fade' : 'Rafael Barber', date: today, time: `${12 + i}:00`, status: i % 3 ? 'Agendado' : 'Confirmado', channel: 'Admin' }))); return s; },
    criticalStock: s => { s.products = s.products.map(p => ({ ...p, quantity: p.id === 'prd-002' ? 5 : p.quantity, stock: p.id === 'prd-002' ? 5 : p.stock, status: p.id === 'prd-002' ? 'Crítico' : p.status })); return s; },
    highRevenue: s => { s.serviceOrders = s.serviceOrders.map(o => ({ ...o, status: 'Paga', paidAt: new Date().toISOString() })); return s; },
    campaignMode: s => { s.campaigns.push({ id: 'cmp-002', name: 'Cashback Relâmpago', audience: 'Clientes com saldo', period: '02/06 a 09/06', channel: 'WhatsApp', status: 'Ativa', result: 'R$ 4.800 previstos', impacted: 180, estimatedRevenue: 4800, funnel: '180 > 110 > 42 > 21' }); return s; },
    kioskDemo: s => { s.appointments.unshift({ id: 'apt-kiosk-001', client: 'Walk-in Totem', service: 'Corte Masculino', professional: 'Diego Fade', date: today, time: 'Agora', status: 'Check-in', channel: 'Totem' }); return s; },
    emptyDay: s => { s.appointments = []; s.serviceOrders = []; return s; },
    vipClients: s => { s.clients = s.clients.map(c => ({ ...c, segment: 'VIP', status: 'VIP', vip: 'Sim', cashback: money(c.cashback) + 30 })); return s; },
    stockEmergency: s => { s.products = s.products.map(p => ({ ...p, quantity: Math.min(3, p.quantity), stock: Math.min(3, p.stock), status: 'Crítico', suggestion: 'Reposição urgente hoje' })); return s; }
  };

  function seedForScenario(name = localStorage.getItem(SCENARIO_KEY) || 'default') { return updateDashboardCounters((scenarioMutators[name] || scenarioMutators.default)(clone(baseSeed)), false); }
  function readState() { try { const state = JSON.parse(localStorage.getItem(STORAGE_KEY) || 'null'); return state && typeof state === 'object' ? updateDashboardCounters({ ...seedForScenario(), ...state }, false) : seedForScenario(); } catch { return seedForScenario(); } }
  function writeState(state) { const next = updateDashboardCounters(state, false); localStorage.setItem(STORAGE_KEY, JSON.stringify(next)); window.dispatchEvent(new CustomEvent('barbersync:demo-store-changed', { detail: clone(next) })); return next; }
  function get(moduleName) { return clone(readState()[moduleKey(moduleName)] ?? []); }
  function set(moduleName, data) { const state = readState(); state[moduleKey(moduleName)] = clone(data); writeState(state); return get(moduleName); }
  function add(moduleName, item) { const key = moduleKey(moduleName); const state = readState(); const collection = Array.isArray(state[key]) ? state[key] : []; const next = { id: item?.id || id(key), status: item?.status || 'Ativo', ...(item || {}) }; state[key] = [next, ...collection]; appendTimeline(state, next.clientId, `${next.name || next.client || key} criado em ${key}`); writeState(state); return clone(next); }
  function update(moduleName, itemId, item) { const key = moduleKey(moduleName); const state = readState(); state[key] = (Array.isArray(state[key]) ? state[key] : []).map(current => String(current.id) === String(itemId || item?.id) ? { ...current, ...(item || {}), id: current.id } : current); writeState(state); return get(key).find(current => String(current.id) === String(itemId || item?.id)) || null; }
  function patch(moduleName, itemId, patchValue) { return update(moduleName, itemId, patchValue || {}); }
  function remove(moduleName, itemId) { const key = moduleKey(moduleName); const state = readState(); state[key] = (Array.isArray(state[key]) ? state[key] : []).filter(item => String(item.id) !== String(itemId)); writeState(state); return get(key); }
  function find(moduleName, itemId) { return get(moduleName).find(item => String(item.id) === String(itemId)) || null; }
  function filter(moduleName, predicateName, value) { const list = get(moduleName); const predicates = { byClient: i => i.clientId === value || i.client === value || i.name === value, byStatus: i => i.status === value, critical: i => i.status === 'Crítico' || money(i.stock ?? i.quantity) <= money(i.minimumStock) }; return list.filter(predicates[predicateName] || (() => true)); }
  function reset(moduleName) { const key = moduleKey(moduleName); const state = readState(); state[key] = clone(seedForScenario()[key] ?? []); writeState(state); return get(key); }
  function resetAll() { localStorage.setItem(SCENARIO_KEY, 'default'); const state = seedForScenario('default'); writeState(state); toast('Demo resetada para o cenário padrão.'); return clone(state); }
  function loadScenario(scenarioName) { const name = scenarioMutators[scenarioName] ? scenarioName : 'default'; localStorage.setItem(SCENARIO_KEY, name); const state = seedForScenario(name); writeState(state); toast(`Cenário carregado: ${name}`); return clone(state); }
  function exportState() { return JSON.stringify(readState(), null, 2); }
  function importState(json) { const parsed = typeof json === 'string' ? JSON.parse(json) : json; if (!parsed || typeof parsed !== 'object') throw new Error('Estado demo inválido'); writeState({ ...seedForScenario(), ...parsed }); return readState(); }

  function appendTimeline(state, clientId, message) { if (!message) return; state.timeline = state.timeline || []; state.timeline.unshift({ id: id('tl'), clientId, message, createdAt: new Date().toISOString() }); }
  function enrichLink(item, moduleName) { if (moduleName === 'appointment') { const client = find('clients', item.clientId) || get('clients').find(c => c.name === item.client); const service = find('services', item.serviceId) || get('services').find(s => s.name === item.service); const pro = find('professionals', item.professionalId) || get('professionals').find(p => p.name === item.professional); return { ...item, clientId: item.clientId || client?.id, serviceId: item.serviceId || service?.id, professionalId: item.professionalId || pro?.id, client: item.client || client?.name, service: item.service || service?.name, professional: item.professional || pro?.name }; } return item; }
  function createLinkedAppointment(clientId, serviceId, professionalId, options = {}) { const state = readState(); const client = state.clients.find(c => c.id === clientId) || state.clients[0]; const service = state.services.find(s => s.id === serviceId) || state.services[0]; const professional = state.professionals.find(p => p.id === professionalId) || state.professionals[0]; const appointment = { id: id('apt'), clientId: client.id, serviceId: service.id, professionalId: professional.id, client: client.name, service: service.name, professional: professional.name, date: options.date || today, time: options.time || '15:30', status: options.status || 'Agendado', channel: options.channel || 'Admin', timeline: ['Agendamento criado'] }; state.appointments.unshift(appointment); appendTimeline(state, client.id, `Agendamento criado para ${service.name} às ${appointment.time}.`); writeState(state); return clone(appointment); }
  function createServiceOrderFromAppointment(appointmentId) { const state = readState(); const appointment = state.appointments.find(a => a.id === appointmentId); if (!appointment) return null; let order = state.serviceOrders.find(o => o.appointmentId === appointmentId); if (!order) { const service = state.services.find(s => s.id === appointment.serviceId) || state.services.find(s => s.name === appointment.service) || { name: appointment.service, price: 0 }; order = { id: id('so'), appointmentId, clientId: appointment.clientId, serviceId: service.id, professionalId: appointment.professionalId, name: `Comanda #${String(state.serviceOrders.length + 1001)}`, client: appointment.client, service: service.name, professional: appointment.professional, items: [{ type: 'service', name: service.name, amount: money(service.price) }], subtotal: money(service.price), discount: 0, cashback: 0, total: money(service.price), paymentMethod: 'PIX', status: 'Aberta', channel: appointment.channel || 'Admin' }; state.serviceOrders.unshift(order); } appointment.status = 'Comanda aberta'; appendTimeline(state, appointment.clientId, `Comanda aberta para ${order.service}.`); writeState(state); return clone(order); }
  function addProductToOrder(serviceOrderId, productId, quantity = 1) { const state = readState(); const order = state.serviceOrders.find(o => o.id === serviceOrderId); const product = state.products.find(p => p.id === productId) || state.products[0]; if (!order || !product) return null; order.items = Array.isArray(order.items) ? order.items : []; order.items.push({ type: 'product', productId: product.id, name: product.name, amount: money(product.price), quantity }); order.subtotal = order.items.reduce((sum, item) => sum + money(item.amount) * money(item.quantity || 1), 0); order.total = Math.max(0, order.subtotal - money(order.discount) - money(order.cashback)); writeState(state); return clone(order); }
  function payServiceOrder(serviceOrderId, payment = {}) { const state = readState(); const order = state.serviceOrders.find(o => o.id === serviceOrderId); if (!order) return null; order.paymentMethod = payment.method || payment.paymentMethod || order.paymentMethod || 'PIX'; order.discount = money(payment.discount ?? order.discount); order.cashback = money(payment.cashback ?? order.cashback); order.total = Math.max(0, money(order.subtotal || order.total) - order.discount - order.cashback); order.status = 'Paga'; order.paidAt = new Date().toISOString(); order.receiptNumber = `REC-${Date.now().toString().slice(-6)}`; (order.items || []).filter(i => i.type === 'product' || i.productId).forEach(item => reduceStock(state, item.productId, money(item.quantity || 1), `Venda ${order.name}`)); const client = state.clients.find(c => c.id === order.clientId || c.name === order.client); if (client) { client.totalSpent = money(client.totalSpent) + order.total; generateCashbackInternal(state, client.id, order.total); appendTimeline(state, client.id, `Comanda ${order.name} paga via ${order.paymentMethod}.`); } state.receipts.unshift({ id: id('rec'), serviceOrderId: order.id, number: order.receiptNumber, total: order.total, method: order.paymentMethod, createdAt: order.paidAt }); generateReviewInternal(state, order.clientId, order.id, false); writeState(state); return clone(order); }
  function reduceStock(state, productId, quantity, reason) { const product = state.products.find(p => p.id === productId); if (!product) return; product.quantity = Math.max(0, money(product.quantity ?? product.stock) - quantity); product.stock = product.quantity; product.status = product.quantity <= money(product.minimumStock) ? 'Crítico' : 'Normal'; product.suggestion = product.status === 'Crítico' ? `Comprar ${Math.max(12, money(product.minimumStock) * 2)} unidades` : 'Estoque saudável'; state.stockMovements.unshift({ id: id('mov'), productId, product: product.name, type: 'Saída', quantity, reason, createdAt: new Date().toISOString() }); }
  function generateReviewInternal(state, clientId, serviceOrderId, persist = true) { const client = state.clients.find(c => c.id === clientId) || {}; const review = { id: id('rev'), clientId, serviceOrderId, name: client.name || 'Cliente demo', rating: 5, nps: 10, comment: 'Avaliação demo gerada após pagamento.', status: 'Promotor' }; state.reviews.unshift(review); appendTimeline(state, clientId, 'Avaliação NPS 10 registrada.'); if (persist) writeState(state); return review; }
  function generateReview(clientId, serviceOrderId) { return clone(generateReviewInternal(readState(), clientId, serviceOrderId)); }
  function generateCashbackInternal(state, clientId, amount) { const cashbackAmount = Math.round(money(amount) * 0.05 * 100) / 100; const client = state.clients.find(c => c.id === clientId); if (client) client.cashback = Math.round((money(client.cashback) + cashbackAmount) * 100) / 100; let loyalty = state.loyalty.find(l => l.clientId === clientId); if (!loyalty && client) { loyalty = { id: id('loy'), clientId, name: client.name, cashback: 0, expiring: 0, status: 'Saldo ativo' }; state.loyalty.unshift(loyalty); } if (loyalty) loyalty.cashback = Math.round((money(loyalty.cashback) + cashbackAmount) * 100) / 100; appendTimeline(state, clientId, `Cashback gerado: R$ ${cashbackAmount.toFixed(2).replace('.', ',')}.`); return cashbackAmount; }
  function generateCashback(clientId, amount) { const state = readState(); const value = generateCashbackInternal(state, clientId, amount); writeState(state); return value; }
  function updateDashboardCounters(state = readState(), persist = true) { const appointments = state.appointments || []; const orders = state.serviceOrders || []; const paid = orders.filter(o => o.status === 'Paga'); const critical = (state.products || []).filter(p => p.status === 'Crítico' || money(p.stock ?? p.quantity) <= money(p.minimumStock)); const reviews = state.reviews || []; const avg = reviews.length ? reviews.reduce((s, r) => s + money(r.rating), 0) / reviews.length : 0; const channelRevenue = paid.reduce((acc, o) => { acc[o.channel || 'Admin'] = money(acc[o.channel || 'Admin']) + money(o.total); return acc; }, { Admin: 0, PublicWeb: 0, Totem: 0, Mobile: 0 }); state.dashboard = { kpis: [
      { label: 'Receita hoje', value: `R$ ${paid.reduce((s, o) => s + money(o.total), 0).toLocaleString('pt-BR')}`, trend: '+ demo' }, { label: 'Agendamentos hoje', value: String(appointments.length), trend: 'ao vivo' }, { label: 'Clientes ativos', value: String((state.clients || []).length), trend: '+ base' }, { label: 'Comandas abertas', value: String(orders.filter(o => ['Aberta', 'Comanda aberta'].includes(o.status)).length), trend: 'PDV' }, { label: 'Estoque crítico', value: String(critical.length), trend: 'repor' }, { label: 'Avaliação média', value: avg.toFixed(1).replace('.', ','), trend: '⭐' }, { label: 'Campanhas ativas', value: String((state.campaigns || []).filter(c => c.status === 'Ativa').length), trend: 'CRM' }, { label: 'Cashback gerado', value: `R$ ${(state.loyalty || []).reduce((s, l) => s + money(l.cashback), 0).toLocaleString('pt-BR')}`, trend: 'fidelidade' }, { label: 'Conversão PublicWeb', value: `${18 + appointments.filter(a => a.channel === 'PublicWeb').length}%`, trend: 'leads' }, { label: 'Atendimentos pelo Totem', value: String(appointments.filter(a => a.channel === 'Totem').length), trend: 'fila menor' }, { label: 'Clientes reativados', value: String((state.campaigns || []).length * 8), trend: 'recorrência' }, { label: 'Receita por canal', value: `A ${channelRevenue.Admin}|P ${channelRevenue.PublicWeb}|T ${channelRevenue.Totem}|M ${channelRevenue.Mobile}`, trend: 'mix' }
    ], flow: { scheduled: appointments.length, checkin: appointments.filter(a => a.status === 'Check-in').length, service: appointments.filter(a => a.status === 'Em atendimento').length, orders: orders.filter(o => o.status === 'Aberta').length, paid: paid.length, reviews: reviews.length }, alerts: critical.map(p => `${p.name} abaixo do mínimo`).concat(['Copilot encontrou horários vazios amanhã']).slice(0, 5), opportunities: ['Criar campanha de retorno', 'Oferecer combo premium', 'Gerar reposição automática'] };
    if (persist) writeState(state); return state; }

  window.BarberSyncDemoStore = { get, set, add, update, patch, remove, find, filter, reset, resetAll, loadScenario, exportState, importState, createLinkedAppointment, createServiceOrderFromAppointment, addProductToOrder, payServiceOrder, generateReview, generateCashback, updateDashboardCounters, updateStatus: (moduleName, itemId, status) => patch(moduleName, itemId, { status }), seed: clone(baseSeed), moduleKey, enrichLink };
})();
