(()=>{'use strict';const root=document.querySelector('[data-client-portal-admin]'),status=document.querySelector('[data-admin-status]');if(!root||!status)return;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){status.textContent='Carregando dados da unidade…';try{const response=await fetch('/AdminApi/client-portal/admin/dashboard',{credentials:'same-origin'});const payload=await response.json();if(!response.ok)throw Object.assign(new Error(payload.detail||'Não foi possível carregar.'),{traceId:payload.traceId});const data=payload.data;root.innerHTML=`<article><h2>Acessos recentes</h2><strong>${data.recentAccesses.length}</strong><p>eventos auditáveis</p></article><article><h2>Atendimento e NPS</h2><strong>${data.reviews.length}</strong><p>avaliações recebidas</p></article><article><h2>Solicitações</h2><strong>${data.openSupport.length}</strong><p>aguardando equipe</p></article><article><h2>Pagamentos</h2><strong>${data.pendingPayments}</strong><p>pendências reais</p></article>`;status.innerHTML=data.openSupport.length?`<strong>Fila de suporte</strong>${data.openSupport.map(x=>`<p>${esc(x.clientName)} · ${esc(x.subject)} · ${esc(x.status)}</p>`).join('')}`:'Nenhuma solicitação aberta nesta unidade.'}catch(e){status.textContent=`${e.message}${e.traceId?` Código de ajuda: ${e.traceId}`:''}`}}load()})();
