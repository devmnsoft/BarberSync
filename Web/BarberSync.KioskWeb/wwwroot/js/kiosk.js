(async()=>{const c=document.getElementById('kioskServices');if(!c)return;try{const r=await fetch('http://localhost:8080/api/kiosk/services?deviceCode=KIOSK-DEMO-001');const j=await r.json();const arr=j.data||[];c.innerHTML=arr.map(s=>`<div class='col-md-4'><button class='btn btn-outline-light btn-lg w-100'>${s.name||'Serviço demo'}<br/><small>${s.durationMinutes||30} min</small></button></div>`).join('')}catch{c.innerHTML='<div class="alert alert-warning">Modo offline amigável ativo.</div>';}})();
