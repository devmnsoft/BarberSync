(()=>{const form=document.querySelector('[data-store-checkout]');if(!form)return;form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('[data-checkout-status]');status.textContent='Criando pedido aguardando pagamento…';try{const response=await fetch('/api/club/online-sales/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const body=await response.json();if(!response.ok)throw new Error(body.detail||'Não foi possível criar o pedido.');status.textContent='Pedido criado. Aguardando pagamento conforme as instruções do estabelecimento.';}catch(error){status.textContent=error.message;}});})();
